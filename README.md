This code does not render on Github.  Reason for this is Git does not support NODE.js + express + EJS options.

This runs great locally. renders a webpages and does an API call to get the
information from another site.

This uses, Express, Node.js, EJS for rendering the page.
One must initilize NPM and install Express, Node, and EJS.

enjoyed created this along my journey

TaterDale

